var win =Ti.UI.currentWindow;

var firstWindow = Ti.UI.createWindow({
	backgroundColor: "white"
});

var dataWindow = Ti.UI.createView({
	width: "80%",
	height: 20,
	color: "blue"
});

firstWindow.add(dataWindow);
firstWindow.open();

